import numpy as np

''' 
- broadcasting allows Numpy to perfom operations on arrays
- with diffrent shapes by virtually expanding dimensions
- so they match the larger array's shape

the dimensions have the same size OR one of the dimensions has a size of 
'''
